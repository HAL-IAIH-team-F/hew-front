"use client"
import {useEffect} from "react";

export default  function Page(
  {}: {}
) {
  useEffect(() => {
    // noPromptUrl(redirectUrl.toString()).then(value => {
    // })
  }, []);
  return <div>
  </div>
}
